#pragma once

namespace reef {
/*
 * @brief Represents a source file and its associated compilation state.
 *
 * Contains tokens, the AST, caches, and other information used by the
 * runtime.
 */

struct Module { };
} // namespace reef